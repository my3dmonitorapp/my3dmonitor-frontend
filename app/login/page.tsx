export default function LoginPage() {
  return (
    <main style={{ maxWidth: 700, margin: "40px auto", padding: 16, fontFamily: "system-ui" }}>
      <h1>Login</h1>
      <p>Auth (Google + email) will be added next. For now we continue building the UI + onboarding.</p>
      <p><a href="/app">Go to Dashboard</a></p>
    </main>
  );
}
